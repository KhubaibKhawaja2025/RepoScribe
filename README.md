# RepoScribe

Landing page and README generator UI for RepoScribe (React, Vite, Tailwind CSS).

## Scripts

- `npm run dev` — local development server
- `npm run build` — production build
- `npm run preview` — preview production build
